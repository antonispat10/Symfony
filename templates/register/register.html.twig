{% extends 'base.html.twig' %}

{%  block body %}

    {% for flashMessage in app.session.flashbag.get('register') %}

        {{ flashMessage }}

    {% endfor %}

    {{  form_start(form) }}

    {{  form_widget(form) }}

    {{ form_end(form) }}

{%  endblock %}
